import React from "react";
import LoginForm from "../auth/LoginForm";

const LoginPage = () => {
  return (
    <div className="login">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
